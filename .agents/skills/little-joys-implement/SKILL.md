---
name: little-joys-implement
description: Execute an authorized Little Joys runtime sprint with two Builder stages and one independent Critic, respecting shared worktrees and verified playtest delivery.
---

Read root `AGENTS.md`, `docs/AI_WORKFLOW.md`, `docs/TOY-STUDIO-WORKFLOW.md` and the complete owning packet. Inspect actual branch/HEAD/dirty ownership and its stage ledger. If interrupted, resume the existing stage. A small settled task needs a proportionate packet, not a new Party session. Documentation/setup is outside the runtime cycle.

- **B1:** The accountable builder implements the accepted outcome directly, with bounded helpers as appropriate. Self-check/repair, run required checks, inspect actual motion/presentation, and freeze source/build identity plus evidence and AC gaps for review.
- **C1:** Dispatch exactly one independent read-only critic on that candidate. Supply the packet, sanitized feedback, source and evidence. Use the workflow scorecard; require reproduction and evidence for issue IDs and distinguish risks/hypotheses. No critic patches or sub-panel. If independent critique cannot run, disclose it; self-review does not fulfill C1.
- **B2:** Return to the same builder, disposition every issue and run affected plus required final checks. Record final identity and remaining boundaries; B1's independent score does not apply to changed bytes. No automatic C2 or B3. An explicitly authorized exception retains prior counts and a bounded remainder.

Keep scope/file ownership and process ownership explicit. Do not deploy through another lane's active integration. Back up sanitized scoped work, and release only within the existing authorization and deployment policy. Provide exact pending device checks and a few optional human feedback prompts when delivering an actual playtest. Passing tests does not establish enjoyment or physical-device qualification.
