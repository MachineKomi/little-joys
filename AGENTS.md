# Little Joys agent entry point

Read [docs/AI_WORKFLOW.md](docs/AI_WORKFLOW.md) and the current task's owning packet before substantial design or implementation. Follow current user scope first; [docs/SPEC.md](docs/SPEC.md) and accepted amendments own the product. Resolve live branch, HEAD, dirty files and ownership before writing. Historical handoff status is not proof of current deployment or another agent's completion.

Use `little-joys-design` for unresolved play ideas and substantive creative planning; skip Party for settled fixes. Use `little-joys-implement` for authorized runtime sprints: **Builder 1 → one independent read-only Critic → same Builder 2**. Track actual stages; resume interruptions without resetting counts. No automatic C2/B3. Routine documentation and tooling setup do not manufacture a runtime sprint. User-authorized continuation can explicitly change a bounded stage budget.

Prefer real independent specialists for substantial design debate, and disclose any single-agent fallback. Never claim self-review or scripted personas are independent. BMAD designs; the accountable builder writes runtime code directly. Do not invoke BMAD Build/auto-build/dev-story orchestration. Use available requested Spark helpers for bounded routine tasks, with lead review; never silently substitute models or make extra user-owned tasks for helpers.

One writer per packet across B1/B2. Preserve foreign dirty work and current integration ownership; do not reset, switch, merge, stage or kill another lane's work/processes. Serialize shared expensive browser/performance runs. Setup is prospective: do not restart or impose new review counts on the ongoing release integration.

Keep family identities, diagnoses, raw transcripts/photos and private play observations in ignored local storage. Public docs, agent/image prompts, logs and commits must be sanitized. Party memory stays off. Record neutral decisions, real dissent, stage/evidence identities and recoverable checkpoints in the repository; do not publish raw personal exchanges. Ignore files are not remote backup.

Preserve imperative Canvas2D, semantic React shell, bounded resources, silent default, reduced-motion and all stop paths. Never infer physical iPad performance or player enjoyment from desktop checks. Follow [DEPLOYMENT.md](DEPLOYMENT.md): scoped backups independently of releases; material input change plus stable version increase for a deliberate playtest build. Tooling/docs-only work does not bump or deploy the app.
